diff --git a/src/app/dashboard/unified-activity-panel.tsx b/src/app/dashboard/unified-activity-panel.tsx
index f087692..4f2d6be 100644
--- a/src/app/dashboard/unified-activity-panel.tsx
+++ b/src/app/dashboard/unified-activity-panel.tsx
@@ -621,12 +621,7 @@ export function UnifiedActivityPanel({
           the rail's own scope card label right below it. Full Queue/All
           Runs moved into the toolbar row instead of getting a whole row
           of their own. */}
-      {/* glass-visible-border: this outer shell's border is otherwise the
-          same near-invisible dark-mode border every surface-glass-2 card
-          gets (globals.css) — barely there against the rail/table busy-ness
-          right up against it. Opt-in modifier, scoped to this card only;
-          doesn't touch the shared class queue-panel.tsx's own card uses. */}
-      <div ref={containerRef} className="glass-visible-border surface-glass-2 rounded-lg overflow-visible flex flex-col md:flex-row min-h-[420px] w-full">
+      <div ref={containerRef} className="surface-glass-2 rounded-lg overflow-visible flex flex-col md:flex-row min-h-[420px] w-full">
         {/* LEFT RAIL — By Worker / By Category. No client dimension: this
             workspace has exactly one client (see EngagementActionsMenu's
             own "one-workspace-one-client" comment) so there's nothing an
@@ -851,36 +846,25 @@ export function UnifiedActivityPanel({
             </div>
           </div>
 
-          {/* Content-area background — NOT queue-panel.tsx's literal
-              `dark:bg-sidebar` (tried that; it's a no-op in dark mode).
-              globals.css: --sidebar is #100c19, and .surface-glass-2 (the
-              outer shell) resolves to --card (#1c1729) color-mixed to 62%
-              opacity over the page's near-black --background — those two
-              land close enough that a --card-based fix (tried first)
-              wasn't reliably distinguishable either. Using the standard
-              zinc scale instead (zinc-100/zinc-900) — a well-known,
-              unambiguous step away from the shell's near-black tone,
-              already proven visible elsewhere in this exact file (row
-              hover/selected states below use zinc-800/zinc-900 the same
-              way) — rather than another custom-token guess. zinc-900/65
-              reads noticeably darker than the header/rail chrome without
-              going flat black — the near-black --background underneath
-              still shows through at that opacity. Light mode reuses the
-              rail's own `#f8f7fa` so both sides of the card read as one
-              surface instead of two different off-whites. */}
-          <div className="flex-1 min-w-0 overflow-y-auto max-h-[560px] bg-[#f8f7fa] dark:bg-zinc-900/65 p-3">
+          {/* Content-area background — literally the rail's own two classes
+              (`bg-[#f8f7fa] dark:bg-sidebar`), not an approximation. Both
+              sides of the card now read as the same dark tone in dark
+              mode instead of the table reading lighter than the rail. */}
+          <div className="flex-1 min-w-0 overflow-y-auto max-h-[560px] bg-[#f8f7fa] dark:bg-sidebar p-3">
             {pagedItems.length === 0 ? (
               <div className="h-full flex flex-col items-center justify-center py-12 text-center text-zinc-500 dark:text-zinc-500 space-y-1">
                 <p className="text-sm font-medium">{items.length === 0 ? "Nothing to show yet." : sharedToolbarCopy.noResultsTitle}</p>
                 {items.length > 0 && <p className="text-xs font-mono text-zinc-400 dark:text-zinc-600">{sharedToolbarCopy.noResultsSubtitle}</p>}
               </div>
             ) : (
-              // No surface-glass-1 here — that class's own ambient radial
-              // glow (globals.css) is exactly the glow being dimmed; a
-              // plain rounded/clip wrapper over the dimmed background
-              // above avoids it without touching that shared class (used
-              // as-is by queue-panel.tsx and everywhere else).
-              <div className="rounded-md overflow-hidden">
+              // The card that actually houses the execution/queue rows —
+              // no surface-glass-1 (that class's own ambient radial glow is
+              // exactly what got dimmed away earlier), just a plain visible
+              // border so this list reads as its own contained card, distinct
+              // from the outer merge shell around rail+table. Tighter radius
+              // than the outer shell (rounded-md vs. the shell's rounded-lg)
+              // on purpose — this is the inner card, not the outer one.
+              <div className="rounded-md overflow-hidden border border-zinc-300 dark:border-zinc-700">
                 {pagedItems.map((item) => {
                   const isBusy = busyId === item.queueItem?.id;
                   const isTriggering = triggeringId === item.id;
diff --git a/src/app/globals.css b/src/app/globals.css
index 756da0a..d1ae5c5 100644
--- a/src/app/globals.css
+++ b/src/app/globals.css
@@ -437,24 +437,6 @@
   background: var(--surface-glass);
 }
 
-/* Opt-in to a clearly visible border on a surface-glass-* card, for the
-   rare outer "card frame" that needs to read as a distinct container
-   (not blend into the page). The dark-mode surface-glass-* border is
-   deliberately near-invisible by design (see the 2026-09-10 note above)
-   for surfaces meant to blend in — this is the explicit opt-in for the
-   handful that shouldn't. Pair with a surface-glass-* class rather than
-   replacing it; does not change the shared surface-glass-* rule itself. */
-.glass-visible-border.surface-glass-1,
-.glass-visible-border.surface-glass-2,
-.glass-visible-border.surface-glass-3 {
-  border-color: rgba(0, 0, 0, 0.14);
-}
-.dark .glass-visible-border.surface-glass-1,
-.dark .glass-visible-border.surface-glass-2,
-.dark .glass-visible-border.surface-glass-3 {
-  border-color: rgba(255, 255, 255, 0.16);
-}
-
 /* Flat solid surfaces (buttons) that still need real depth without a
    glass fill — shadow tiers alone, no blur/translucency. */
 .shadow-elevation-1 {
